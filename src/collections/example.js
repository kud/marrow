/**
 * Tweets
 * @type {Collection}
 */
define([
    'backbone',
    'helper',
    'models/tweet'
], function( Backbone, Helper, Tweet ) {

    var Tweets = Backbone.Collection.extend({

    });

    return Tweets;
});
