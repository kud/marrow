/**
 * Tweet
 * @type {Model}
 */
define([
    'backbone',
    'moment',
    'momentFr',
    'helper'
], function( Backbone, Moment, MomentFr, Helper ) {

    var Tweet = Backbone.Model.extend({

    });

    return Tweet;
});
